package model.items;

import model.Coord;
import model.Environment;
import model.UnusableItem;

public class Rocher extends UnusableItem{
	
	public Rocher(Coord position, Environment env) {
		super(position, env);
	}
}
