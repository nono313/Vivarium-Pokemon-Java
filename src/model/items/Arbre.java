package model.items;

import model.Coord;
import model.Environment;
import model.UnusableItem;

public class Arbre extends UnusableItem{

	public Arbre(Coord position, Environment env) {
		super(position, env);
	}

}
